# For local dev, leave this unset — Vite proxies /api to localhost:5000.
# For production (Vercel), set this to your deployed Render API URL, e.g.:
# VITE_API_URL=https://aqi-predictor-api.onrender.com/api
VITE_API_URL=